
<% if is_representative? || is_admin? || is_leader? %>
	<% if @operation.modality == "FCL - EXW" %>
		<%= render "operations/fcl_exw/fcl_frame", operation: @operation %>
	<% elsif @operation.modality == "LCL" %>
		<%= render "operations/lcl/lcl_frame", operation: @operation %>
	<% end %>

<% elsif is_agent? %>
<% end %>

