<% if is_admin? %>
	<%= render 'admin_index' %>
<% elsif is_agent? %>
	<%= render 'agent_index' %>
<% elsif is_consignee? %>
	<%= render 'consignee_index' %>
<% elsif is_shipper? %>
	<%= render 'shipper_index' %>
<% end %>


<%=link_to('Logout', destroy_user_session_path, method: :delete)%>