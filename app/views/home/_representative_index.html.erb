<div class="section">
	<%= render 'shared/tabs_buttons', role: 'representative', role_to_register: 'agent' %>
	<%= render 'shared/tabs_content', role: 'representative', role_to_register: 'agent' %>
</div>

